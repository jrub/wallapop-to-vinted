[pytest]
# Default discovery picks up any function starting with "test", which collides
# with domain functions like ``testid_to_key``. Require the underscore.
python_functions = test_*
markers =
    playwright: tests that drive a Playwright browser (require pytest-playwright + Chromium)
